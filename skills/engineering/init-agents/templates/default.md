# <project name>

<One paragraph: what this project is and does, for whom. From the README if it says it well.>

## Commands

<Only commands that exist in the repo's config. Drop rows that don't apply.>

| Task | Command |
|---|---|
| Install | `<command>` |
| Build | `<command>` |
| Test | `<command>` |
| Lint | `<command>` |
| Run | `<command>` |

## Layout

<Top-level directories that matter and what lives in each — the map a fresh session needs. Skip self-explanatory entries.>

## Conventions

<Rules in force that an agent must follow and can't infer from tooling alone: style choices the linter doesn't enforce, patterns to copy, things to avoid. If tooling enforces it, don't repeat it here.>

## Vault

This repo's knowledge lives in the memory-layer vault project `<project>` — search it before starting work (`search_vault`); track bugs and tasks there (`create_issue` / `list_issues`).
